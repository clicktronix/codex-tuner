interface:
  display_name: "Specify Task"
  short_description: "Define DoR, tests, acceptance, and DoD"
  default_prompt: "Use $codex-tuner:spec to turn this task into a committed executable spec with DoR, tests, acceptance, and DoD."
policy:
  products:
    - "CODEX"
  allow_implicit_invocation: false
