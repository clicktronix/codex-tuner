interface:
  display_name: "Run Specification"
  short_description: "Execute a committed spec through merge"
  default_prompt: "Use $codex-tuner:run to execute this committed specification through verified delivery."
policy:
  allow_implicit_invocation: false
