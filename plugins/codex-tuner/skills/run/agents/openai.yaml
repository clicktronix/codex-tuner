interface:
  display_name: "Run Task"
  short_description: "Deliver a task through native Goal mode"
  default_prompt: "Use $codex-tuner:run to implement this task through tests, proportionate review, required CI, merge, and cleanup."
policy:
  products:
    - "CODEX"
  allow_implicit_invocation: false
