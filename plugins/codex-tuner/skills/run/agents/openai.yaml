interface:
  display_name: "Run Specification"
  short_description: "Run a spec through exact-SHA delivery"
  default_prompt: "Use $codex-tuner:run to publish a plan and execute this committed spec through tested, exact-SHA reviewed delivery."
policy:
  products:
    - "CODEX"
  allow_implicit_invocation: false
